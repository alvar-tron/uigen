Your goal is to update anyt vulnerable dependencies.

Do the following:
1. Run `npm audit` to find vulnerable installes packages in this project.
2. Run `npm audit fix` to applu updates.
3. Run tests and verigy the updates didn't breack anything.
